import type { Meta, StoryObj } from '@storybook/react'

import HeaderContentFooterLayout from './HeaderContentFooterLayout'

const meta: Meta<typeof HeaderContentFooterLayout> = {
  component: HeaderContentFooterLayout,
}

export default meta

type Story = StoryObj<typeof HeaderContentFooterLayout>

export const Primary: Story = {}
